#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<<< HEAD:ReactJS/Aula 24 - Intro ao React/node_modules/.bin/webpack
  exec "$basedir/node"  "$basedir/../webpack/bin/webpack.js" "$@"
else 
  exec node  "$basedir/../webpack/bin/webpack.js" "$@"
========
  exec "$basedir/node"  "$basedir/../typescript/bin/tsserver" "$@"
else 
  exec node  "$basedir/../typescript/bin/tsserver" "$@"
>>>>>>>> fdb5256 (Aula 23 - Proj. ClimaApp: Modulo 03):Proj. Front-end Dinâmico/Aula 23 - Proj. ClimaApp/node_modules/.bin/tsserver
fi
